<%= simple_form_for [@reviewed_user, @review] do |f| %>
      <%= f.input :rating, collection: 1..5 %>
      <%= f.input :content %>
      <%= f.button :submit, class: "btn-orange-sm" %>
    <% end %>
